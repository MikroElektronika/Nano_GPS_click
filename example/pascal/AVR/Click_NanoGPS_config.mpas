unit Click_NanoGPS_config;

uses Click_NanoGPS_types;

const
    _NANOGPS_TIMER_LIMIT : uint32_t = 5; // 5 ticks
    _NANOGPS_BUF_WARNING : uint16_t = 192; // 192 bytes activate warning
    _NANOGPS_POLL_ENABLE : uint8_t = 1; // poll enabled
    _NANOGPS_CALLBACK_ENABLE : uint8_t = 0; // calback disabled
    _NANOGPS_UART_CFG : array[3] of uint32_t = (4800, _UART_NOPARITY, _UART_ONE_STOPBIT); 
    
end.